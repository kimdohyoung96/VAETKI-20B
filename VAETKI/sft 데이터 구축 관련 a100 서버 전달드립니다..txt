sft 데이터 구축 관련 a100 서버 전달드립니다.

1. 모델이 정상적으로 떠 있는지 확인하려면 curl을 실행해주세요.
curl http://172.20.92.134:30940/v1/models

2. 추론 확인하려면 하기 코드 실행해주세요.

from openai import OpenAI

client = OpenAI(
    base_url="http://172.20.92.134:30940/v1",
    api_key="EMPTY"
)

r = client.chat.completions.create(
    model="google/gemma-4-31B-it",
    messages=[
        {
            "role": "user",
            "content": "안녕하세요. 자기소개 한 줄로 해주세요."
        }
    ],
    max_tokens=100,
    temperature=0.2,
)

print(r.choices[0].message.content)



----------------



conda 키기위해, 가상환경 진입

source /kdhms/miniconda3/etc/profile.d/conda.sh

conda activate gemma4

rm -f ~/vllm_server.log

CUDA_VISIBLE_DEVICES=0,1 nohup python -m vllm.entrypoints.openai.api_server \
  --model google/gemma-4-31B-it \
  --host 0.0.0.0 \
  --port 8888 \
  --tensor-parallel-size 2 \
  --gpu-memory-utilization 0.92 \
  --max-model-len 1024 \
  --max-num-seqs 1 \
  --max-num-batched-tokens 1024 \
  --enforce-eager \
  > ~/vllm_server.log 2>&1 & disown

tail -f ~/vllm_server.log